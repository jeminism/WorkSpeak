# WorkSpeak Client-Side Agent
# Requirements for local message interception and rewriting

# Core dependencies
requests>=2.31.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
numpy>=1.26.0

# LLM Integration
sentence-transformers>=2.3.0

# Platform-specific dependencies
# Windows
pywin32>=306; platform_system == "Windows"
uiautomation>=2.0.14; platform_system == "Windows"
pyautogui>=0.9.54; platform_system == "Windows"

# macOS
# pyautogui works cross-platform, no additional setup needed

# Linux
python-xlib>=0.33; platform_system == "Linux"

# UI / GUI
pyobjc; platform_system == "Darwin"  # macOS AppKit

# Optional: OCR for accessibility fallback
pytesseract>=0.3.10; platform_system == "Linux" or platform_system == "Windows"
Pillow>=10.0.0

# Testing
pytest>=7.4.0
